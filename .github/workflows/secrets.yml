name: Secure Workflow

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Use Secret in a script
      run: |
        echo $API_KEY , $API_USER
      env:
        API_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
        API_USER: ${{ secrets.EC2_USER }}
